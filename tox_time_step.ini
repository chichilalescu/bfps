[tox]
envlist = py27
[testenv]
whitelist_externals =
    echo
    cp
passenv = LD_LIBRARY_PATH
changedir =
    {envtmpdir}
commands =
    cp -r {toxinidir}/tests {envtmpdir}
    python tests/test_time_step.py \
        -n 32 \
        --run \
        --initialize \
        --ncpu 2 \
        --nparticles 32 \
        --niter_todo 16 \
        --precision single \
        --wd "data/single"
    python tests/test_time_step.py \
        -n 32 \
        --run \
        --initialize \
        --ncpu 2 \
        --nparticles 32 \
        --niter_todo 16 \
        --precision double \
        --wd "data/double"

